# aquifer-to-dcs

To use, first copy sample.env to .env and enter your Aquifer API Key.

Then setup a python virtual environment:

```bash
python3 -m venv venv
source venv/bin/activate
```

The install the requirements:

```bash
pip install -r requirements.txt
```

You can then run the various scripts with `python <script>.py`.
